spring.application.name=desdeaqui

# Railway inyecta el puerto por variable de entorno; en local cae al 8081
server.port=${PORT:8081}

# En Railway estos valores vienen de las variables que configures en el dashboard.
# En local, si no las definís, usa los valores por defecto de tu MySQL local.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/desdeaqui}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Root123_}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.thymeleaf.cache=false

spring.sql.init.mode=never
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB